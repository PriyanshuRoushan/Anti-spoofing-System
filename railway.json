{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "env": {
      "PYTHON_VERSION": "3.11"
    }
  },
  "deploy": {
    "startCommand": "uvicorn main:app --log-level info --access-log --host"
  }
}